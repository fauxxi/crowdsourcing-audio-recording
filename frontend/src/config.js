export const config = {
	minDuration: 1, // in seconds
	storageLimit: '5368709120', // in byte according aws s3 free tier
	labels: ['window noise', 'laughter', 'people talking'], // add the labels
};
